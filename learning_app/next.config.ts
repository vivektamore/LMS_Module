import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // App Router body size limits are controlled per-route via route segment config,
  // not here. The 'api' key is Pages Router only.
};

export default nextConfig;
